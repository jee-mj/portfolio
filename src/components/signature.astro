---
import MainText from "../content/signature/main-text.md";
import MainTitle from "../content/signature/main-title.md";
---

<section
  class="w-full h-full flex-col z-20 hidden bg-[#22222288]"
  id="signature"
>
  <div
    class="w-full h-full p-4 pb-0 text-white transition-all duration-600 inline-block overflow-y-auto z-20"
    id="signature-content"
  >
    <div class="mb-8 ml-4">
      <div class="pb-8 pt-4 sm:pb-4">
        <div class="mx-auto">
          <div class="grid grid-cols-1 items-center gap-x-8 gap-y-8">
            <div class="xl:flex xl:flex-row">
              <div class="mx-auto w-full xl:mx-0">
                <div class="text-3xl font-bold tracking-tight text-white">
                  <MainTitle />
                </div>
                <div class="mt-6 mb-16 text-lg leading-8 text-white">
                  <MainText />
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div
    class="w-full h-[36vh] flex flex-row-reverse p-4 transition-all duration-500 z-50"
    id="signature-label"
  >
    <a
      style="font-size: clamp(2em, 5vw, 4em); padding-right: clamp(0.5em,10vw,60em);"
      class="hover:border-[#ffcd00] hover:text-[#ffcd00]
        transition-all duration-500 hover:w-full
        font-smooch border border-solid border-white
        w-1/2 h-full
        grid content-center justify-end
        bg-secondary text-white shadow-sm shadow-black"
      id="signature-link">jee-mj</a
    >
    <div id="signature-space" class="h-full w-auto bg-[#22222288]"></div>
  </div>
</section>

<style>
  html,
  body {
    height: 100%;
    margin: 0;
    padding: 0;
  }
</style>
<script>
  const signatureLink = document.getElementById("signature-link");

  // Event listener for section load
  document.addEventListener("sectionLoadEvent", function () {
    setTimeout(() => {
      signatureLink.classList.remove("w-1/2");
      signatureLink.classList.remove("hover:w-full");
      signatureLink.classList.add("w-full");
      setTimeout(() => {
        document
          .getElementById("signature-content")
          .classList.remove("text-white");
      }, 150);
    }, 500);
  });

  // Event listener for section unload
  document.addEventListener("sectionUnloadEvent", function () {
    document.getElementById("signature-content").classList.add("text-white");
    signatureLink.classList.remove("w-full");
    signatureLink.classList.add("w-1/2");
    setTimeout(() => {
      signatureLink.classList.add("hover:w-full");
    }, 1500);
    document.getElementById("signature-label").classList.add("w-full");
    document.getElementById("signature-label").classList.remove("w-1/2");
  });
</script>
